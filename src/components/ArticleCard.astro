---
interface Props {
  title: string;
  description: string;
  category: string;
  date: Date;
  slug: string;
  leadImage?: string;
}

const { title, description, category, date, slug, leadImage } = Astro.props;

// Generate responsive image paths from lead image path
const thumbImage = leadImage?.replace(/lead\.webp$/, 'lead-thumb.webp');
const mediumImage = leadImage?.replace(/lead\.webp$/, 'lead-medium.webp');

const formattedDate = date.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'short',
  day: 'numeric'
});
---

<a href={`/writing/${slug}`} class={`article-card ${leadImage ? 'has-image' : ''}`}>
  {leadImage && (
    <div class="card-image">
      <img
        src={thumbImage}
        srcset={`${thumbImage} 400w, ${mediumImage} 600w, ${leadImage} 1400w`}
        sizes="(max-width: 768px) 220px, 200px"
        alt={title}
        loading="lazy"
        decoding="async"
        width="400"
        height="200"
      />
    </div>
  )}
  <div class="card-content">
    <span class="category">{category}</span>
    <h3>{title}</h3>
    <p class="description">{description}</p>
    <span class="date">{formattedDate}</span>
  </div>
</a>

<style>
  .article-card {
    display: block;
    padding: var(--space-lg);
    border: 1px solid var(--border);
    border-radius: 8px;
    text-decoration: none;
    color: inherit;
    transition: border-color 0.2s ease;
  }

  .article-card.has-image {
    display: grid;
    grid-template-columns: 200px 1fr;
    gap: var(--space-lg);
  }

  .article-card:hover {
    border-color: var(--border-hover);
  }

  .article-card:hover h3 {
    color: var(--accent);
  }

  .card-image {
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 6px;
    background-color: var(--border);
    overflow: hidden;
    height: 140px;
    min-height: 140px;
    max-height: 140px;
  }

  .card-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }

  .article-card:hover .card-image img {
    transform: scale(1.03);
  }

  .card-content {
    display: flex;
    flex-direction: column;
  }

  .category {
    display: inline-block;
    font-size: var(--text-tag);
    font-weight: 500;
    color: var(--accent);
    margin-bottom: var(--space-xs);
  }

  h3 {
    font-family: var(--font-heading);
    font-size: var(--text-h3);
    font-weight: 500;
    margin-bottom: var(--space-xs);
    color: var(--text-primary);
    transition: color 0.2s ease;
  }

  .description {
    font-size: 15px;
    color: var(--text-secondary);
    margin-bottom: var(--space-sm);
    line-height: 1.5;
    flex-grow: 1;
  }

  .date {
    font-size: var(--text-meta);
    color: var(--text-tertiary);
  }

  @media (max-width: 768px) {
    .article-card.has-image {
      grid-template-columns: 1fr;
      gap: var(--space-md);
    }

    .card-image {
      height: 160px;
      min-height: 160px;
      max-height: 160px;
    }
  }
</style>
