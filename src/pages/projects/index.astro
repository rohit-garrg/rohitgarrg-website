---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Projects - Rohit Garg"
  description="Side projects, experiments, and hobby builds."
>
  <div class="container">
    <header class="page-header">
      <h1>Projects</h1>
      <p class="page-description">Side projects, experiments, and things I'm tinkering with.</p>
    </header>

    <div class="placeholder-state">
      <div class="placeholder-icon">
        <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <path d="M12 19V5"/>
          <path d="M5 12l7-7 7 7"/>
        </svg>
      </div>
      <h2>Coming Soon</h2>
      <p>I'm working on a few side projects that I'll share here once they're ready. Check back soon, or follow along on <a href="https://www.linkedin.com/in/rohitgarrg/" target="_blank" rel="noopener noreferrer">LinkedIn</a> for updates.</p>
    </div>
  </div>
</BaseLayout>

<style>
  .page-header {
    padding: var(--space-xl) 0;
    border-bottom: 1px solid var(--color-border);
    margin-bottom: var(--space-xl);
  }

  .page-header h1 {
    margin-bottom: var(--space-xs);
  }

  .page-description {
    color: var(--color-text-light);
    font-size: 1.125rem;
    margin-bottom: 0;
  }

  .placeholder-state {
    text-align: center;
    padding: var(--space-2xl) var(--space-lg);
    border: 2px dashed var(--color-border);
    border-radius: 12px;
    margin-bottom: var(--space-xl);
  }

  .placeholder-icon {
    color: var(--color-border);
    margin-bottom: var(--space-md);
  }

  .placeholder-state h2 {
    font-size: 1.5rem;
    margin-bottom: var(--space-sm);
    color: var(--color-text);
  }

  .placeholder-state p {
    color: var(--color-text-light);
    max-width: 400px;
    margin: 0 auto;
    line-height: 1.6;
  }

  .placeholder-state a {
    font-weight: 500;
  }

  @media (max-width: 768px) {
    .page-header {
      padding: var(--space-lg) 0;
    }

    .placeholder-state {
      padding: var(--space-xl) var(--space-md);
    }
  }
</style>
